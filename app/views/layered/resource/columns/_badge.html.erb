<% variant = (!value.nil? && options.fetch(:variants, {})[value.to_s.to_sym]) || :default %>
<span class="l-ui-badge l-ui-badge--<%= variant %><%= " l-ui-badge--rounded" if options[:rounded] %>"><%= value %></span>
